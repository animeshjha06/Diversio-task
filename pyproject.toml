[project]
name = "diversio-task"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Animesh Jha", email = "animeshjha95166@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "pytest>=9.1.1",
]

[project.scripts]
diversio-task = "diversio_task:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
